! ============================================================
! DEVICE: PC-C
! LAB:    ITN Lab 3 — Task 2
! ROLE:   End host on Router R3 LAN
! ============================================================

! ── Static IPv4 ───────────────────────────────────────────
! Windows (elevated command prompt):
netsh interface ip set address name="Local Area Connection" static 172.30.30.3 255.255.255.0 172.30.30.1

! Verify:
ipconfig /all

! ── Connectivity Tests ────────────────────────────────────
ping 172.30.30.1        ! R3 default gateway
ping 10.1.1.2           ! R3 serial interface
ping 10.1.1.1           ! R1 serial interface
ping 172.30.10.1        ! R1 LAN gateway
ping 172.30.10.3        ! PC-A (requires static route on R3 AND R1)
